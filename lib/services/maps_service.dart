import 'package:latlong2/latlong.dart';

/// Retained for backward-compatibility with files that reference
/// [MapsService.defaultLocation].
class MapsService {
  static const defaultLocation = LatLng(30.0444, 31.2357);
}
